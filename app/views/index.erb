<div class='container'>
  <h3>Welcome HackerNews Clone</h3>
  <% @posts.each do |post|%>
    <p><a href='<%= post.url %>'><%= post.title %></a></p>
    <p><a href="/posts/<%= post.id %>">Comments</a></p>
  <% end %>
</div>
